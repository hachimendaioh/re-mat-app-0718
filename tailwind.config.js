/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // ★この行を追加または確認★
    "./public/index.html",         // 必要であれば追加
  ],
  theme: {
    extend: {},
      colors: {
        // ここにカスタムカラーを定義します
        // 'gray-800' の定義を上書きして、より浅い色にします
        'gray': { // Tailwindのデフォルトのgrayパレットを拡張
          // ... (他のgrayのシェードはそのまま残すか、必要に応じて変更)
          800: '#3A2E5B', // ここを #2C204B より浅い色に変更
          // 例: 900: '#1A032E', // 非常に深い紫（メイン背景の開始色）
        },
        // 既存のカスタムカラー（もしあれば）
        'purple': {
          '600': '#8B5CF6', // 例: デフォルトのTailwindパープル
          '700': '#7C3AED',
          // ...
          '400': '#A78BFA', // 編集ボタンなどで使用
        },
        'card-bg': '#2C204B', // カードの背景色として明示的に定義することも可能
        'accent-pink': '#FF007F', // SWCアプリのアクセントカラー
        // ... 他のカスタムカラー ...
      },
  },
  plugins: [],
}